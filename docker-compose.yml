version: '3.8'

services:
  mongo:
    image: mongo:6.0
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: meanapp
    volumes:
      - mongo_data:/data/db

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.backend
    image: yourdockerhub/mean-backend:latest
    environment:
      - PORT=8080
      - MONGODB_URI=mongodb://mongo:27017/meanapp
    depends_on:
      - mongo
    expose:
      - "8080"
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.frontend
    image: yourdockerhub/mean-frontend:latest
    expose:
      - "80"
    restart: unless-stopped

  nginx-proxy:
    image: nginx:stable-alpine
    volumes:
      - ./nginx-proxy/default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend
    restart: unless-stopped

volumes:
  mongo_data:
<<<<<<< HEAD

=======
>>>>>>> 61b1a754bfcf8ae9953b593442d2a49dafe27402
